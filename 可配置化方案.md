# FDE 交付流程图 · 可配置化方案

> 目标:把「阶段 → 步骤 → 活动 → 产出物 / 出门禁」全链路内容从源码中剥离,变成一份**可编辑、可校验、可版本化**的配置文件——不碰代码即可增删改流程内容。

## 目录

- 0. 现状诊断
- 1. 设计原则
- 2. 配置 Schema 设计(核心)
- 3. 页面与模块设计
- 4. 编辑器功能优先级
- 5. 实施路线
- 6. 关键决策点(需拍板)
- 7. 风险与对策
- 8. 工作量估算

---

## 0. 现状诊断(基于 index.html 代码事实)

| 层 | 现状 | 问题 |
|---|---|---|
| 内容数据 | `DB` 对象(L276-533):阶段 s1-s7 / 打法 / 概念 / 坑位 k1-k7+zj 四类实体详情 | 与渲染代码同文件,改内容 = 改源码 |
| 结构数据 | `MAP` 数组(L538-630):7 阶段 × L2 步骤卡 × L3 活动行,行上挂 `out`(产出物)、`warn`(坑位)、`id`(详情引用) | 无 schema 约束,改错一行 JS 全页白屏 |
| 渲染层 | `renderMap()` 读 MAP 拼 HTML | 结构字段名写死在模板字符串里 |
| 交互层 | 搜索 / 缩放 / 抽屉弹层 / 快捷导航 / 移动端 / 打印 | 质量高,值得原样保留 |
| 配置度 | 半配置:已数据驱动,但无编辑界面、无校验、无版本、无导入导出 | — |

**结论**:底子是好的(已是数据驱动),缺的只是**配置外置 + 编辑器 + 校验**三件事,不需要推倒重来。

---

## 1. 设计原则

1. **数据与渲染彻底分离**:内容全部进一份配置文件,页面零硬编码。
2. **渐进改造**:保留 index.html 全部交互能力(搜索/缩放/抽屉/移动端/打印)。
3. **零依赖**:不加任何框架和构建工具,纯原生 HTML/JS,双击即可打开。
4. **单一事实来源**:一份配置同时供「查看页」和「编辑器」使用,导出即成品。
5. **引用可校验**:活动引用的方法/坑位必须存在,渲染前统一校验。

---

## 2. 配置 Schema 设计(核心)

### 2.1 层级模型

```
L1 阶段 stage ── L2 步骤 step ── L3 活动 activity ── 产出物 output / 出门禁 gate
```

关键变化(对照现状):

| 配置点 | 现状 | 可配置化后 |
|---|---|---|
| 阶段 | MAP 顶层对象 | `stages[]`:增删、排序、改色、改别名 |
| 步骤 | cards 数组 | `steps[]`:任意增删排序,编号自动重算 |
| 活动 | rows 数组(text+out+warn) | `activities[]`:text / methodRef / outputs[] / gate{} / pitfallRefs[] |
| 产出物 | 单个字符串 chip | `outputs[]` 数组:多产出物、可标 required、可挂模板链接 |
| 出门禁 | 仅阶段级 1 句话 | 阶段级 `gate{}` 结构化(判据 + 检查清单 + 证据);活动级可选 gate(完成标准) |
| 方法/坑位库 | DB 对象 | `methods{}` / `pitfalls{}` 独立区块,被引用而非内联 |
| 横切线/页脚 | 硬编码 HTML | `lanes[]` / `footer` 进配置 |

### 2.2 Schema 示例(节选自现有真实内容)

```json
{
  "schemaVersion": "1.0",
  "meta": { "title": "FDE 交付全流程拆解图", "subtitle": "L1→L4 · 交互版", "generatedAt": "2026-09-07" },

  "stages": [
    {
      "id": "s3",
      "no": "3",
      "title": "POC 方案与最小验证",
      "alias": "五阶段:POC 方案 · 六步法:做 MVD",
      "color": "c3",
      "goal": "用最小成本验证「该不该做、能不能做」…",
      "gate": {
        "id": "G3", "label": "验证门",
        "text": "MVD 四段闭环跑通,80% 可用即验证",
        "checklist": [
          { "item": "闭环四段(输入→AI→人工确认→回写)真实跑通", "evidence": "MVD 演示记录" },
          { "item": "80% 可用达标", "evidence": "评测数据" }
        ]
      },
      "outputs": ["数据盘点表", "POC 方案书(九要素)", "MVD 最小闭环"],
      "note": "高保真 demo 不是本阶段目标…",
      "steps": [
        {
          "id": "s3-step4", "no": "3.4", "title": "MVD 最小闭环实施",
          "activities": [
            {
              "id": "s3-4-1", "code": "3.4.1",
              "text": "闭环四段:输入 → AI 处理 → 人工确认 → 回写业务",
              "methodRef": "mvd",
              "outputs": [ { "name": "MVD 最小闭环", "required": true } ],
              "gate": { "text": "四段缺一段即不算 MVD" },
              "pitfallRefs": ["k1"]
            }
          ]
        }
      ]
    }
  ],

  "methods":  { "mvd": { "cat": "概念", "t": "MVD 最小可行交付闭环", "src": "…", "one": "…", "how": ["…"], "ex": "…", "rel": ["lj","le"] } },
  "pitfalls": { "k1":  { "t": "坑1 · Demo 到生产翻车", "sym": "…", "cause": "…", "fix": "…", "rel": ["sc","gh"] } },
  "lanes":    [ { "id": "resp", "icon": "🧭", "title": "责任线(贯穿 G1–G7)", "body": "…", "color": "#b91c1c" } ],
  "footer": "来源与置信度…"
}
```

### 2.3 为什么用 config.js 而不是 config.json

`file://` 协议下 `fetch('config.json')` 会被浏览器 CORS 拦截,双击打开即白屏。改用 `data/config.js`:

```js
window.FDE_CONFIG = { /* 同上 JSON */ };
```

查看页与编辑器都用 `<script src="data/config.js">` 引入——零服务、零依赖、双击可用,与本项目零依赖静态页路线一致。(将来若部署到 Pages/服务器,可平滑换回 JSON + fetch。)

---

## 3. 页面与模块设计

```
FDE_WorkFlow_Book/
├── index.html          # 现状保留(快照版),或改造为读配置(见 §6 二选一)
├── app.html            # 新查看页:读 config 渲染,交互能力 = index.html
├── editor.html         # 编辑器:树形导航 + 表单编辑 + 校验 + 导入导出
├── data/
│   └── config.js       # 单一事实来源(编辑器导出即覆盖此文件)
```

### 3.1 查看页 app.html

- 渲染逻辑从 index.html 平移,数据源从 `DB / MAP` 改为 `FDE_CONFIG`;
- 增强:阶段门禁弹层渲染 `checklist`(判据逐条 + 证据);活动存在 gate 时显示「完成标准」小节;
- 搜索 / 缩放 / 抽屉 / 移动端 / 打印行为保持一致。

### 3.2 编辑器 editor.html(核心新模块)

布局:左树 + 右表单 + 顶部工具栏。

| 区域 | 功能 |
|---|---|
| 左侧树 | 阶段 → 步骤 → 活动三级树;点选定位;按钮增删、上移/下移 |
| 右侧表单 | 按选中节点类型渲染字段表单(阶段 / 步骤 / 活动 / 门禁 / 产出物分组) |
| 方法/坑位库 | 独立 tab 管理 methods / pitfalls,供活动下拉引用 |
| 顶部工具栏 | 导入 / 导出 JSON / 导出 config.js / 校验 / 预览 / 撤销重做 |

---

## 4. 编辑器功能优先级

| 优先级 | 功能 | 说明 |
|---|---|---|
| P0 | 树形导航 + 表单编辑全部字段 | 覆盖 5 类配置点 |
| P0 | 增删节点、上移/下移排序 | 阶段/步骤/活动三级 |
| P0 | 编号自动重算 | 删除步骤 3.2 后,原 3.3/3.4 自动顺延 |
| P0 | 导入/导出(JSON + config.js) | 导出文件直接替换 data/config.js |
| P0 | localStorage 自动草稿 | 防误关页面丢内容 |
| P1 | 引用校验 | methodRef / pitfallRefs 悬空即红色高亮 + 汇总列表 |
| P1 | 预览模式 | 编辑器内一键切到渲染视图 |
| P1 | 撤销/重做 | 快照栈(每次结构变更存一份) |
| P2 | 拖拽排序 | 替代上移/下移 |
| P2 | 多配置方案 profile | 「标准版 / 精简版 / 客户版」切换 |
| P2 | 导出 Markdown | 配置导出为可读文档 |

---

## 5. 实施路线

| 阶段 | 内容 | 交付物 | 验收标准 |
|---|---|---|---|
| Phase 1 | 现有 DB+MAP 迁移为 data/config.js;app.html 改造渲染源 | config.js + app.html | app.html 与 index.html 内容渲染一致 |
| Phase 2 | editor.html P0 功能 | editor.html | 从零搭出「阶段→步骤→活动→产出物/门禁」并导出生效 |
| Phase 3 | 校验 + 预览 + 撤销(P1) | 增强版编辑器 | 悬空引用全量拦截 |
| Phase 4(可选) | 持久化升级:导出替换 + git 提交(默认)/ 接 Gitee/GitHub API 自动提交 / 小型 Node 服务 | 按需 | — |

---

## 6. 关键决策点(需拍板)

| 决策 | 选项 A | 选项 B | 建议 |
|---|---|---|---|
| index.html 去留 | 保留为静态快照,新能力只进 app.html | 直接改造 index.html 读配置 | **A**:快照稳定可用,风险隔离 |
| 配置载体 | data/config.js(script 引入) | config.json(fetch) | **A**:双击打开不挂 |
| 编辑器形态 | 独立 editor.html | app.html 内置编辑模式 | **先 A**:独立页简单清晰,后续可合并 |
| 排序方式 | 上移/下移按钮 | 拖拽 | **先按钮后拖拽**:实现稳、移动端可靠 |

---

## 7. 风险与对策

| 风险 | 对策 |
|---|---|
| schema 后续演进(加字段) | schemaVersion 字段;渲染器宽容策略:未知字段忽略、缺失字段给默认值 |
| 误改配置导致页面异常 | 渲染前校验 + 错误报告;index.html 快照兜底 |
| 编辑器复杂度失控 | 严格按 P0→P2 优先级,P0 不做拖拽/撤销 |
| 方法/坑位库被误删(仍被引用) | 删除前检查引用计数,非 0 弹提示 |

---

## 8. 工作量估算

| 阶段 | 估算 |
|---|---|
| Phase 1 迁移 + 查看页 | 约半天(逻辑平移为主) |
| Phase 2 编辑器 P0 | 约 1 天 |
| Phase 3 校验/预览/撤销 | 约半天 |
